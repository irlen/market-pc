import { combineReducers } from 'redux'
import windowH from './windowH'
import lookset from './lookset'
import loginInfo from './loginInfo'

export default combineReducers({
  windowH,
  lookset,
  loginInfo
})
